<resources>

    <style name="PopupMenuListView" parent="@style/Widget.AppCompat.ListView.DropDown">
        <item name="android:divider">@color/newColorBgDark</item>
        <item name="android:dividerHeight">1dp</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/newColorBg</item>
        <item name="colorPrimaryDark">@color/newColorBgDark</item>
        <item name="colorAccent">@color/newColorElement</item>

        <item name="android:textColor">@color/newColorText</item>
        <item name="android:windowBackground">@color/newColorBg</item> <!--Per bottoni timer -->
        <item name="android:drawableTint">@color/newColorElement</item> <!-- Per bottoni '+' e '-' -->
        <item name="android:colorBackgroundFloating">@color/newColorText</item> <!-- Per bottoni impostazioni -->
        <item name="android:iconTint">?android:attr/windowBackground</item>
        <item name="android:dropDownListViewStyle">@style/PopupMenuListView</item>
        <item name="android:actionBarStyle">@style/ToolbarTheme</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_baseline_arrow_back_24</item>

    </style>

    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/newColorElement</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.buttonTimerDark" parent="buttonTimer">
        <item name="android:tint">?android:attr/windowBackground</item>
        <item name="android:background">@color/newColorElement</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:textColor">@color/newColorElement</item>
        <item name="android:colorBackground">@color/newColorBgDark</item>
    </style>

    <style name="AlertDialogStyleDark" parent="Theme.AppCompat.Dialog">
        <item name="android:textColorPrimary">@color/newColorBg</item>
    </style>
    <!--_____________________________________________________________________________________________-->

    <style name="buttonTimer">
        <item name="android:layout_gravity">bottom|end</item>
        <item name="android:background">@drawable/ripple_effect</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>

  </resources>